
-- The name of the branch with the highest total orders

-- Tables involved= Orders, Branches
-- Output = Branch ID, Branch Name, Number of Orders (Calculated field)

SELECT BNAME FROM (SELECT 

